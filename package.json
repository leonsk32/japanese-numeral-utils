{
  "name": "@leonsk32/japanese-numeral-utils",
  "version": "0.3.3",
  "description": "Utilities for handling Japanese numerals.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepublish": "tsc",
    "test": "jest --testPathPattern=specs --runInBand"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/leonsk32/japanese-numeral-utils"
  },
  "author": "leonsk32",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js",
      "json"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/specs/**/?(*.)+(spec|test).[jt]s?(x)"
    ]
  }
}
